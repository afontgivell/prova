{% extends 'base.html.twig' %}

{% block body %}
<div>
    {{ op1 }} {{ operator }} {{ op2 }} = {{ result }}
</div>
<div>
    <a href="{{ path('app_calculator_index') }}">return</a>
</div>
{% endblock %}
